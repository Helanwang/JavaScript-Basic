let a, b, c;
a = 10;
b = 20;
c = a + b;
// JS ignores white space
console.log(c);


// numbers and strings
nums = [10, 10.1]
string = ['John']

// const keywords means the variable is immutable(unchangeable)

console.log('Hello' + ' ' + 'Helen')
//concatenate of the strings

//JS is case-sensitive.
// these are two different variable because of the capitalization
let LastName, lastname;

LastName = 'jack'
lastname = 'ryan'

console.log(LastName + lastname)

/* This is multi-line docstrings */

// let, var and const
// only use let for variable.
//Only use var if you MUST support old browsers.

$ = "Dollar"
console.log($)

//JavaScript treats a dollar sign as a letter, valid variable names

_ = 'Happy'
console.log(_)

//JavaScript treats underscore as a letter, _ are valid variable names